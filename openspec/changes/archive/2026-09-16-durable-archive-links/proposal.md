## Why

PR #15 renders archive paths as relative hrefs, which resolve beneath the PR page rather than the repository file tree. Issue #16 installs the corrected dad publication instructions and records evidence for durable archive links.

## What Changes

- Refresh the nine installed dad skill directories from corrected source and verify exact bytes while preserving unrelated workflows.
- Reproduce the relative-href defect and validate absolute, commit-pinned candidates against an already published archive.
- Document archive-first publication, matching links across four surfaces, and coordinator checks before merge and after child-branch deletion.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

None. This is installed workflow documentation and evidence only; `skip_specs: true` avoids adding dad behavior to game specifications.

## Impact

Only the installed dad payload and issue-specific evidence change. The corrected source currently differs in `shared/pr-body.md` and `issue/single-branch/merge.md`. Gameplay, browser tests, dependencies, and historical merged PR bodies remain outside scope. Existing full-suite and strict specification gates remain required.
